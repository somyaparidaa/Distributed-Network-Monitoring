apiVersion: 1

providers:
  - name: 'Distributed Network Monitor'
    orgId: 1
    folder: 'Network Telemetry'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards/json

